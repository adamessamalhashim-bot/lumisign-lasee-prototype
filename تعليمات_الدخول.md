# تشغيل تسجيل الدخول في LumiSign

الملفات المطلوبة في GitHub:

1. ارفع `portal_app.py` في المجلد الرئيسي للمستودع.
2. استبدل ملف `requirements.txt` بالنسخة الجديدة المرفقة.
3. في إعدادات تطبيق Streamlit غيّر **Main file path** إلى `portal_app.py`.
4. اختر Python 3.11.

## إعداد Google Login

1. افتح Google Cloud Console وأنشئ مشروعًا أو استخدم مشروعًا موجودًا.
2. افتح **Google Auth Platform** ثم أكمل شاشة الموافقة OAuth.
3. اختر نوع المستخدم **External**، وأدخل اسم التطبيق `LumiSign` وبريد الدعم.
4. أنشئ OAuth Client من نوع **Web application**.
5. أضف رابط إعادة التوجيه التالي في **Authorized redirect URIs**:

   `https://lumisign-lasee-prototype-adamessam.streamlit.app/oauth2callback`

6. انسخ `Client ID` و`Client secret`.
7. من صفحة تطبيقك في Streamlit اختر **Manage app → Settings → Secrets** والصق:

```toml
[auth]
redirect_uri = "https://lumisign-lasee-prototype-adamessam.streamlit.app/oauth2callback"
cookie_secret = "ضع_هنا_نصاً_عشوائياً_طويلاً_جداً"
client_id = "ضع_Google_Client_ID_هنا"
client_secret = "ضع_Google_Client_Secret_هنا"
server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"
```

8. احفظ Secrets ثم أعد تشغيل التطبيق.

## الصلاحيات

- الأدمن: `adamessam.alhashim@gmail.com`
- أي حساب Google آخر: تجربة وتحليل الإشارة فقط.
- لا ترفع `Client secret` إلى GitHub ولا ترسله لأحد.
